server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  instance:
    prefer-ip-address: true
    hostname: eureka-service
  client:
    register-with-eureka: false
    fetch-registry: false